// Package analytics carries the read + rollup use-cases for the daily
// analytics pipeline. It orchestrates the domain analytics types + the
// two repository ports (AnalyticsRepo and AnalyticsSource) — zero
// provider imports, zero infrastructure imports.
package analytics
