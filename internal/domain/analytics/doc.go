// Package analytics holds the canonical, provider-agnostic shapes used
// by the daily rollup pipeline and the read-side dashboard endpoints.
// It carries zero infrastructure or provider imports — every third-party
// SDK is confined to internal/providers/*.
package analytics
