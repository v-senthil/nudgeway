// Package apitoken models long-lived API tokens used by programmatic
// clients (MCP server, CI, custom scripts) to authenticate against the
// Nudgeway REST API in place of a browser session cookie.
package apitoken
