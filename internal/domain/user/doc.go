// Package user models the platform users — the agents and admins who log
// into the fullWA UI. Distinct from Contact, which represents the end
// customer on the other side of the conversation.
package user
