// Package middleware provides the HTTP middleware chain: request-ID,
// structured logging, panic recovery, session auth, CSRF, RBAC, tenant
// scoping, and per-org rate limiting.
package middleware
